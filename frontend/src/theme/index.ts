import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#18BA51",
      contrastText: "#fff",
    },
    secondary: {
      main: "#000000",
    },
  },
  spacing: 8,
});
